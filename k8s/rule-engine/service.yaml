apiVersion: v1
kind: Service
metadata:
  name: rule-engine
  namespace: kafka-sigma-engine
spec:
  clusterIP: None
  selector:
    app: rule-engine
  ports:
    - port: 8001
      targetPort: 8001
      name: metrics
